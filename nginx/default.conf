server {
    listen 80;
    server_name api.shreekamalnayanfrozenfood.com;
    return 301 https://$host$request_uri;
}

server {
    listen 443 ssl http2;
    server_name api.shreekamalnayanfrozenfood.com;

    ssl_certificate /etc/letsencrypt/live/api.shreekamalnayanfrozenfood.com/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/api.shreekamalnayanfrozenfood.com/privkey.pem;
    ssl_session_cache shared:SSL:10m;
    ssl_session_timeout 1h;
    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_prefer_server_ciphers on;
    ssl_ciphers HIGH:!aNULL:!MD5;

    location / {
        proxy_pass http://app:5000;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
    }
}
